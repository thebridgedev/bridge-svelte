{
  "name": "nblocks-svelte-workspace",
  "private": true,
  "workspaces": [
    "nblocks-svelte",
    "demo"
  ],"scripts": {
    "build": "bun run --filter demo build",
    "build:nblocks": "bun run --filter @nebulr/nblocks-svelte build",
    "dev": "bun run --filter demo dev",
    "package": "bun run --filter @nebulr/nblocks-svelte package"    
  }
}
