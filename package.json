{
    "name": "nblocks-svelte",
    "private": true,
    "scripts": {
      "dev": "bun run dev --cwd demo",
      "build": "bun run build --cwd demo",
      "build:nblocks": "bun run build --cwd nblocks-svelte"
    }
}