{
  "name": "bridge-svelte-workspace",
  "private": true,
  "workspaces": [
    "bridge-svelte",
    "demo"
  ],"scripts": {
    "build": "bun run --filter demo build",
    "build:bridge": "bun run --filter @nebulr/bridge-svelte build",
    "dev": "bun run --filter demo dev",
    "package": "bun run --filter @nebulr/bridge-svelte package"    
  }
}
